const finalParticipents = ["Taylor","Donald","Don","Natcha","Bobby"]

const announcements = finalParticipents.map((member) => {
    return member + " joined the contest"
})

console.log(announcements)